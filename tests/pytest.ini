[pytest]
testpaths = tests
addopts = -v --tb=short

markers =
    unitario: Testes unitários sem banco de dados
    banco: Testes que requerem banco ferroflux_test
    integracao: Testes de integração entre módulos
